<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
        <script src="https://cdn.jsdelivr.net/npm/@tailwindcss/browser@4"></script>
</head>
<body>
    <div class="w-full h-screen bg-zinc-900 text-white p-10">
        <div class=" w-full flex justify-end">
            <a class="text-sm px-3 py-2 rounded-md mb-5 inline-block bg-red-500" href="/logout" >Logout</a>
        </div>
        <h3 class="text-3xl "> <span class="font-light">Hello,</span> <%= post.user.name %>👋🏻</h3>
        <h5 class=" mb-5">You can Edit Your Posts</h5>
        <form action="/update/<%= post._id %>" method="post">
         <textarea class=" p-3 w-1/3 bg-transparent resize-none rounded-md border-2 border-zinc-800 block"  name="content" placeholder="Whats on your mind?"> <%= post.content %> </textarea>
            <input class=" w-40 mt-2 px-3 py-2 rounded-md bg-yellow-500 test-black text-sm block" type="submit" value="Edit Post" >
        </form>


</body>
</html>