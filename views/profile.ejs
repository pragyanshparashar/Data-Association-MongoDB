<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
        <script src="https://cdn.jsdelivr.net/npm/@tailwindcss/browser@4"></script>
</head>
<body>
    <div class="w-full h-screen bg-zinc-900 text-white p-10">
        <div class=" w-full flex justify-end">
            <a class="text-sm px-3 py-2 rounded-md mb-5 inline-block bg-red-500" href="/logout" >Logout</a>
        </div>
        <div class="flex items-center gap-5 mb-5">
            <div class="w-20 h-20  rounded-md overflow-hidden"> 
                <img class="w-full h-full object-cover" src="/images/uploads/<%= user.profilepicture %>">
            </div>
        <h3 class="text-3xl "> <span class="font-light">Hello,</span> <%= user.name %>👋🏻</h3>
        </div>
        <h5 class=" mb-5">You can create posts</h5>
        <form action="/posts" method="post">
         <textarea class=" p-3 w-1/3 bg-transparent resize-none rounded-md border-2 border-zinc-800 block"  name="content" placeholder="Whats on your mind?"></textarea>
            <input class=" w-40 mt-2 px-3 py-2 rounded-md bg-blue-500 text-sm block" type="submit" value="Create Post" >
        </form>

      <div class="posts mt-20"> 
         <h2 class="">Your Posts</h2>
       <div class="postsconatiner mt-5">
       <% user.posts.forEach(post => { %>

  <div class="post w-1/3 bg-zinc-800 p-5 mb-4 rounded-md border-[1px] border-zinc-700 ">
      <h4 class="text-yellow-500 text-sm mb-2">@<%= user.username %></h4>
      <p class="text-sm tracking-tight"><%= post.content %></p>
          <small class="mt-2 inline block"><%= post.likes.length %> likes</small>
      <div class="btns flex gap-4 mt-1">
         <a class="text-blue-500" href="/likes/<%= post._id %>">Like</a>
         <a class="text-white-500" href="/edit/<%= post._id %>">Edit</a>
      </div>
  </div>

<% }) %>
      
 </div>
</body>
</html>